#include <stdio.h>



/**
 * main - prints the statment given in the requirement
 * Return: 0 if every thing is okay else a non zero value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
